if [ -f ~/.bashrc ] ; then
    . ~/.bashrc
fi
export PATH=/usr/local/bin:$PATH
export PATH=/usr/local/sbin:$PATH

# Setting myPATH for Vim-Quickrun
PATH="/Users/Satoshi/.vim/latex:${PATH}"
export PATH
export PATH=/usr/local/sbin:$PATH

# beamerのコマンド
export PATH=$PATH:~/.vim/latex
if which pyenv > /dev/null; then eval "$(pyenv init -)"; fi
